# sort_keys для GoogleSheetsWriteOperator

## Overview

В merge-режиме новые строки всегда оказываются в хвосте таблицы (append). Нет способа получить таблицу, отсортированную по ключевой колонке — например, новые даты вверху, старые внизу. Аналогичная проблема актуальна для append и overwrite.

Решение: параметр `sort_keys: list[str] | None = None`. Если задан, после завершения любой записи выполняется один `batchUpdate` с `sortRange` — серверная сортировка на стороне Google Sheets, без чтения данных.

## Context (from discovery)

- **Основной файл:** `airflow_provider_google_sheets/operators/write.py`
- **Тесты:** `tests/test_operators/test_write.py`
- **Затронутые методы:** `__init__`, `execute`, `_execute_overwrite`, `_execute_append`, `_execute_merge` + новый `_execute_sort`
- **Паттерн тестов:** pytest + MagicMock, фикстура `mock_hook` с патчем `GoogleSheetsHook`
- **Существующие `batchUpdate`-вызовы:** `_execute_merge` использует `_batched_batch_update`; `_execute_sort` отправляет один запрос напрямую через `hook.batch_update`

## Development Approach

- **testing approach**: Regular (код + тесты в одном таске)
- Обратная совместимость обязательна: `sort_keys=None` (дефолт) → поведение идентично текущему
- Все тесты должны проходить перед переходом к следующему таску

## Solution Overview

Новый параметр `sort_keys: list[str] | None = None`, формат элементов: `"col:asc"` / `"col:desc"`.

**Валидация в два этапа:**
- `__init__`: формат (наличие `:`, direction = `asc`/`desc`, непустое имя колонки) — ошибки видны при загрузке DAG
- `execute`: существование колонки в `headers` — ошибки видны при запуске

**Метод `_execute_sort`** вызывается в конце каждого режима и отправляет `sortRange`.
Принимает явный `skip_header: bool` вместо чтения `self.has_headers` — каждый call site передаёт правильное значение в зависимости от того, есть ли физически заголовок в листе:

```python
def _execute_sort(self, hook, headers, sheet_id, table_start_col, table_start_row, skip_header: bool, end_row: int):
    sort_specs = []
    start_col_idx = self._column_letter_to_index(table_start_col)
    for spec in self.sort_keys:
        col, direction = spec.split(":", 1)
        col_idx = start_col_idx + headers.index(col.strip())
        sort_specs.append({
            "dimensionIndex": col_idx,
            "sortOrder": "ASCENDING" if direction.strip().lower() == "asc" else "DESCENDING",
        })
    data_start = (table_start_row - 1) + (1 if skip_header else 0)
    hook.batch_update(self.spreadsheet_id, [{
        "sortRange": {
            "range": {
                "sheetId": sheet_id,
                "startRowIndex": data_start,
                "endRowIndex": end_row,
                "startColumnIndex": start_col_idx,
                "endColumnIndex": start_col_idx + len(headers),
            },
            "sortSpecs": sort_specs,
        }
    }])
```

**Место вызова по режимам** — `skip_header` и `end_row` вычисляются на call site:

**overwrite** — вызов после `trim_sheet`:
```python
skip_header = bool(self.write_headers and headers)
end_row = (start_row_num - 1) + len(all_rows)  # all_rows уже включает header-строку, если write_headers=True
self._execute_sort(hook, headers, sheet_id, start_col, start_row_num, skip_header, end_row)
```

**append** — вызов после последнего батча.
Важно: при `sort_keys` (или `write_headers=True`) читаем **полную** первую колонку вместо одной ячейки, чтобы знать `existing_row_count`:
```python
# вместо get_values на одну ячейку — всегда читаем колонку полностью
existing_col = hook.get_values(self.spreadsheet_id, f"{prefix}{start_col}{start_row}:{start_col}")
existing_row_count = len(existing_col)

header_written_this_run = (existing_row_count == 0) and self.write_headers and bool(headers)
# ... запись заголовка если header_written_this_run ...
# ... батчи append ...

if self.sort_keys:
    skip_header = header_written_this_run or (existing_row_count > 0 and self.has_headers)
    end_row = (start_row - 1) + existing_row_count + (1 if header_written_this_run else 0) + len(rows)
    self._execute_sort(hook, headers, sheet_id, start_col, start_row, skip_header, end_row)
```

**merge** — вызов после `repeatCell` (или после блока append если `append_rows` пуст).
Все нужные данные уже есть в методе:
```python
# total_existing уже вычислен на строке 532 кода:
# total_existing = len(existing_keys_raw) + (1 if headers_just_written else 0)
rows_after_merge = total_existing - total_deleted + len(append_rows)
end_row = (table_start_row - 1) + rows_after_merge

# skip_header: заголовок в листе если он уже был (existing_keys_raw непустой) или только что записан
skip_header = bool(existing_keys_raw) or headers_just_written
self._execute_sort(hook, headers, sheet_id, table_start_col, table_start_row, skip_header, end_row)
```

## Technical Details

### Формат параметра

```python
sort_keys=["date:desc", "region:asc"]
```

- Регистр направления нечувствителен: `"ASC"`, `"Asc"`, `"asc"` — всё принимается
- Колонки указываются именами **после** `column_mapping` (те, что реально в таблице)
- **Не добавлять** в `template_fields` — format-валидация в `execute()` несовместима с нерендеренными Jinja-строками

### Валидация в `__init__`

```python
if sort_keys is not None:
    if write_mode == "overwrite" and clear_mode == "range":
        raise ValueError(
            "sort_keys is not compatible with overwrite + clear_mode='range': "
            "sorting across partial column ranges desyncs neighbouring columns"
        )
    for spec in sort_keys:
        if ":" not in spec:
            raise ValueError(f"sort_keys item '{spec}' must be 'column:asc' or 'column:desc'")
        col, direction = spec.split(":", 1)
        if not col.strip():
            raise ValueError(f"sort_keys item '{spec}': column name is empty")
        if direction.strip().lower() not in ("asc", "desc"):
            raise ValueError(
                f"sort_keys item '{spec}': direction must be 'asc' or 'desc', got '{direction.strip()}'"
            )
```

### Валидация колонок и headers в `execute`

Перед вызовом режима — явная проверка (не silent skip через `and headers`):

```python
if self.sort_keys:
    if not headers:
        raise ValueError(
            "sort_keys requires named headers (has_headers=True)"
        )
    for spec in self.sort_keys:
        col = spec.split(":", 1)[0].strip()
        if col not in headers:
            raise ValueError(
                f"sort_keys column '{col}' not found in headers: {headers}"
            )
```

## Implementation Steps

### Task 1: Параметр `sort_keys` в `__init__` и `execute`

**Files:**
- Modify: `airflow_provider_google_sheets/operators/write.py`
- Modify: `tests/test_operators/test_write.py`

- [ ] добавить `sort_keys: list[str] | None = None` в сигнатуру `__init__`
- [ ] добавить `self.sort_keys = sort_keys`
- [ ] **НЕ добавлять** `sort_keys` в `template_fields`: Airflow рендерит шаблоны после `__init__`, поэтому значение вида `"{{ var.value.spec }}"` сломает format-валидацию при загрузке DAG; если в будущем потребуется templating — перенести валидацию в `execute`
- [ ] добавить в `__init__` только type-check: если `sort_keys is not None` и не `list` → `TypeError`
- [ ] добавить валидацию формата (`:` присутствует, direction = asc/desc, имя непустое) в **`execute()`** — после вызова `_resolve_data`, до ветвления по режимам
- [ ] добавить валидацию несовместимости в `__init__`: `sort_keys + write_mode="overwrite" + clear_mode="range"` → `ValueError` (только overwrite — в других режимах `clear_mode` игнорируется)
- [ ] добавить валидацию в `execute()`: `sort_keys` + `has_headers=False` → `ValueError`
- [ ] добавить валидацию в `execute()`: `sort_keys` + `headers is None` (после `_resolve_data`) → `ValueError`
- [ ] обновить docstring класса: описать `sort_keys`, формат элементов, ограничение `overwrite + clear_mode="range"`, требование `has_headers=True`
- [ ] тест: `sort_keys=["date:desc"]` → инициализируется без ошибок
- [ ] тест: `sort_keys` не list → `TypeError` при создании оператора
- [ ] тест: `sort_keys=["date"]` (нет `:`) → `ValueError` при `execute()`
- [ ] тест: `sort_keys=["date:ascending"]` (неверное direction) → `ValueError` при `execute()`
- [ ] тест: `sort_keys=[":desc"]` (пустое имя) → `ValueError` при `execute()`
- [ ] тест: `sort_keys=["date:desc"] + write_mode="overwrite" + clear_mode="range"` → `ValueError` при создании оператора
- [ ] тест: `sort_keys=["date:desc"] + write_mode="append" + clear_mode="range"` → инициализируется без ошибок (clear_mode в append игнорируется)
- [ ] тест: `sort_keys=None` (дефолт) → инициализируется без ошибок
- [ ] запустить тесты — должны проходить перед Task 2

### Task 2: Метод `_execute_sort`

**Files:**
- Modify: `airflow_provider_google_sheets/operators/write.py`
- Modify: `tests/test_operators/test_write.py`

- [ ] реализовать `_execute_sort(self, hook, headers, sheet_id, table_start_col, table_start_row, skip_header: bool, end_row: int)` согласно псевдокоду из Solution Overview
- [ ] убедиться что `endRowIndex` **передаётся** в range равным `end_row` — сортировка только в пределах таблицы, не трогает строки снаружи
- [ ] тест: один ключ `"date:desc"` → `sortRange` с одним spec `sortOrder=DESCENDING`, верный `dimensionIndex`, `endRowIndex` присутствует
- [ ] тест: два ключа `["date:desc", "region:asc"]` → два spec в правильном порядке
- [ ] тест: `skip_header=True` → `startRowIndex` пропускает строку заголовка (`table_start_row - 1 + 1`)
- [ ] тест: `skip_header=False` → `startRowIndex = table_start_row - 1`
- [ ] тест: `end_row=10` → `endRowIndex=10` в payload
- [ ] тест: `table_start_col="C"` → `startColumnIndex=2`, `dimensionIndex` считается от 2, `endColumnIndex=2+len(headers)`
- [ ] тест: direction регистронезависим (`"ASC"`, `"Desc"` → оба принимаются корректно)
- [ ] запустить тесты — должны проходить перед Task 3

### Task 3: Интеграция в `_execute_overwrite`

**Files:**
- Modify: `airflow_provider_google_sheets/operators/write.py`
- Modify: `tests/test_operators/test_write.py`

- [ ] в `_execute_overwrite`: после `trim_sheet` (clear_mode="range" уже заблокирован в __init__) вызывать `_execute_sort` если `self.sort_keys`
- [ ] вычислять на call site: `skip_header = bool(self.write_headers and headers)`, `end_row = (start_row_num - 1) + len(all_rows)`
- [ ] передавать `sheet_id = self._get_sheet_id(hook)`, `table_start_col = start_col`, `table_start_row = start_row_num`
- [ ] тест overwrite + `sort_keys=["name:asc"]` + `write_headers=True` + 5 строк данных → `sortRange.endRowIndex = start_row - 1 + 6` (1 header + 5 data), `startRowIndex` пропускает заголовок
- [ ] тест overwrite + `sort_keys=["name:asc"]` + `write_headers=False` + 5 строк → `endRowIndex = start_row - 1 + 5`, `startRowIndex` без пропуска
- [ ] тест overwrite + `sort_keys=None` → `hook.batch_update` с `sortRange` не вызывается
- [ ] тест: несуществующая колонка в `sort_keys` → `ValueError` в `execute`
- [ ] тест: `column_mapping={"src": "date"}, sort_keys=["date:desc"]` → `sortRange` ищет колонку `"date"` (после маппинга)
- [ ] запустить тесты — должны проходить перед Task 4

### Task 4: Интеграция в `_execute_append`

**Files:**
- Modify: `airflow_provider_google_sheets/operators/write.py`
- Modify: `tests/test_operators/test_write.py`

- [ ] заменить чтение одной ячейки (`get_values` на `{start_col}{start_row}`) на чтение **полной первой колонки** (`{start_col}{start_row}:{start_col}`) — нужно для `existing_row_count` при `sort_keys`; вынести за условие `if self.write_headers and headers` так чтобы вызов происходил всегда когда `sort_keys` или `write_headers`
- [ ] вычислять `existing_row_count = len(existing_col)` и `header_written_this_run = (existing_row_count == 0) and self.write_headers and bool(headers)`
- [ ] после последнего батча, если `self.sort_keys`: вычислять `skip_header = header_written_this_run or (existing_row_count > 0 and self.has_headers)` и `end_row = (start_row - 1) + existing_row_count + (1 if header_written_this_run else 0) + len(rows)`, вызывать `_execute_sort`
- [ ] добавить `sheet_id = self._get_sheet_id(hook)` когда нужно (только если `sort_keys`)
- [ ] тест append на непустой лист + `sort_keys=["date:desc"]` + `has_headers=True` → `sortRange.endRowIndex = existing_count + len(rows)`, `skip_header=True`
- [ ] тест append на пустой лист + `write_headers=True` + `sort_keys` → `skip_header=True`, `endRowIndex = 1 + len(rows)`
- [ ] тест append на пустой лист + `write_headers=False` + `sort_keys` → `skip_header=False`, `endRowIndex = len(rows)`
- [ ] тест append + `sort_keys=None` → `sortRange` не вызывается, `get_values` читает полную колонку только если `write_headers=True`
- [ ] запустить тесты — должны проходить перед Task 5

### Task 5: Интеграция в `_execute_merge`

**Files:**
- Modify: `airflow_provider_google_sheets/operators/write.py`
- Modify: `tests/test_operators/test_write.py`

- [ ] в `_execute_merge`: после `repeatCell` (или после блока append если `append_rows` пуст) вызывать `_execute_sort` если `self.sort_keys`
- [ ] `sheet_id` и `table_start_col/row` уже доступны; вычислять на call site:
  - `total_existing` уже есть в коде (строка 532); `rows_after_merge = total_existing - total_deleted + len(append_rows)`
  - `end_row = (table_start_row - 1) + rows_after_merge`
  - `skip_header = bool(existing_keys_raw) or headers_just_written` (фиксирует баг: `has_headers=True` + `write_headers=False` + пустой лист → `skip_header=False`)
- [ ] тест merge + `sort_keys=["date:desc"]` → `sortRange` вызван последним, `endRowIndex` = корректное значение после delete/append
- [ ] тест merge + `sort_keys=None` → `sortRange` не вызывается
- [ ] тест: merge когда `append_rows` пуст (только deletes) + `sort_keys` → `sortRange` всё равно вызывается, `endRowIndex` учитывает удалённые строки
- [ ] тест: `write_headers=True` + непустой лист → `skip_header=True` (existing_keys_raw непустой)
- [ ] тест: `write_headers=False` + непустой лист → `skip_header=True` (header уже был)
- [ ] тест: `write_headers=False` + пустой лист → `skip_header=False` (headers_just_written=False, existing_keys_raw пустой)
- [ ] запустить тесты — должны проходить перед Task 6

### Task 6: Edge-case тесты и финализация

**Files:**
- Modify: `tests/test_operators/test_write.py`
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `README_RU.md` (если существует)

- [ ] тест: пустые данные после partition (`rows=[]`) + `sort_keys` → `_execute_sort` вызывается с `end_row = table_start_row - 1 + (1 if header_row else 0)`, не падает
- [ ] тест: overwrite с `rows=[]` + `sort_keys` → `sortRange` на диапазон только из header (или пустой диапазон если `write_headers=False`)
- [ ] тест: merge без append и без delete + `sort_keys` → `sortRange` вызывается, `endRowIndex` = существующие строки
- [ ] тест: `sort_keys` + `has_headers=False` → `ValueError` в `execute` (не дожидаясь _execute_sort)
- [ ] запустить полный тест-сьют: `cd /Users/mkozhin/PycharmProjects/airflow-provider-google-sheets && python -m pytest tests/ -v`
- [ ] все тесты зелёные, включая ранее существующие
- [ ] обновить `CHANGELOG.md`: если нет секции `## [Unreleased]` — создать её; добавить в `### Added`: `sort_keys` параметр с кратким описанием
- [ ] обновить `README.md`: добавить `sort_keys` в таблицу параметров `GoogleSheetsWriteOperator`, пример `sort_keys=["date:desc"]`, ограничения (требует `has_headers=True`, несовместим с `overwrite + clear_mode="range"`, сортирует только колонки таблицы)
- [ ] обновить `README_RU.md` аналогично, если файл существует
- [ ] переместить план в `docs/plans/completed/`

## Post-Completion

**Деплой в Airflow:**
- Опубликовать новую версию провайдера и обновить на Airflow-инстансе `realcombi.mgcom.ru`

**Ручная проверка:**
- Запустить DAG с `sort_keys=["date:desc"]` в merge-режиме, убедиться что таблица отсортирована по убыванию после записи
